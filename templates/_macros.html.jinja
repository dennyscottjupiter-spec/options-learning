{% macro tip(text, mode) %}{% if mode == 'learn' %}<span class="tip" tabindex="0"><span class="tip-icon" aria-hidden="true">?</span><span class="tip-text">{{ text }}</span></span>{% endif %}{% endmacro %}
